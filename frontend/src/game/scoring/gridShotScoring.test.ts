import{describe,expect,it}from'vitest';import{comboBonus,consistencyScore,isStable,scoreGridShotHit,speedBonus}from'./gridShotScoring'
describe('transparent Grid Shot scoring',()=>{it('uses interval speed bands',()=>{expect(speedBonus(175).bonus).toBe(50);expect(speedBonus(231).bonus).toBe(30);expect(speedBonus(600).bonus).toBe(0)});it('uses additive combo tiers',()=>{expect(comboBonus(9)).toBe(0);expect(comboBonus(20)).toBe(10);expect(comboBonus(55)).toBe(20)});it('awards stable rhythm',()=>{expect(isStable([200,205,198,203,201])).toBe(true);expect(scoreGridShotHit(200,20,[200,205,198,203,201]).total).toBe(155)});it('scores consistency 0-100',()=>{expect(consistencyScore([200,201,199,202,198])).toBeGreaterThan(95);expect(consistencyScore([])).toBe(0)})})
